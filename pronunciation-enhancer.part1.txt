(() => {
  "use strict";

  const STYLE = `
    :root{
      --an-tissue:#dbeafe;
      --an-tissue-edge:#315b78;
      --an-cavity:#ffffff;
      --an-cavity-edge:#50728a;
      --an-tongue:#ef8d88;
      --an-tongue-edge:#9b4547;
      --an-bone:#fff9e9;
      --an-air:#2563eb;
      --an-label:#31475a;
      --an-contact:#e11d48;
    }
    @media (prefers-color-scheme:dark){
      :root{
        --an-tissue:#233b51;
        --an-tissue-edge:#94bdd8;
        --an-cavity:#101b29;
        --an-cavity-edge:#7fa3bc;
        --an-tongue:#bc6668;
        --an-tongue-edge:#f0a3a1;
        --an-bone:#d8d1bb;
        --an-air:#7aa5ff;
        --an-label:#c7d9e7;
        --an-contact:#fb7185;
      }
    }
    .anatomy-head{opacity:.84;pointer-events:none}
    .an-tissue{fill:var(--an-tissue);stroke:var(--an-tissue-edge);stroke-width:1.15;vector-effect:non-scaling-stroke}
    .an-cavity{fill:var(--an-cavity);stroke:var(--an-cavity-edge);stroke-width:1.05;vector-effect:non-scaling-stroke}
    .an-outline,.an-palate,.an-pharynx,.an-epiglottis,.an-larynx,.an-jawbone{
      fill:none;stroke:var(--an-tissue-edge);stroke-width:1.3;stroke-linecap:round;stroke-linejoin:round;vector-effect:non-scaling-stroke
    }
    .an-palate{stroke-width:1.8}
    .an-tooth{fill:var(--an-bone);stroke:var(--an-tissue-edge);stroke-width:.8;vector-effect:non-scaling-stroke}
    .an-tongue{fill:var(--an-tongue);stroke:var(--an-tongue-edge);stroke-width:1.25;stroke-linejoin:round;vector-effect:non-scaling-stroke}
    .an-lip{fill:var(--an-tongue);stroke:var(--an-tongue-edge);stroke-width:1.1;vector-effect:non-scaling-stroke}
    .an-velum{fill:none;stroke:var(--an-tongue-edge);stroke-width:2;stroke-linecap:round;vector-effect:non-scaling-stroke}
    .an-uvula{fill:var(--an-tongue);stroke:var(--an-tongue-edge);stroke-width:.8;vector-effect:non-scaling-stroke}
    .an-air{fill:none;stroke:var(--an-air);stroke-width:1.5;stroke-linecap:round;stroke-dasharray:3 3;opacity:0;vector-effect:non-scaling-stroke}
    .an-speaking .an-oral-air{opacity:.75;animation:anFlow .55s linear infinite}
    .an-speaking .an-vocal-folds{animation:anBuzz .12s ease-in-out infinite alternate;transform-origin:64px 91px;transform-box:fill-box}
    .an-vocal-folds{fill:none;stroke:var(--an-contact);stroke-width:1.5;stroke-linecap:round;vector-effect:non-scaling-stroke}
    .an-labels line{stroke:var(--an-label);stroke-width:.65;opacity:.72;vector-effect:non-scaling-stroke}
    .an-labels text{fill:var(--an-label);font-size:3.1px;font-family:system-ui,sans-serif;font-weight:650}
    .an-place{fill:var(--an-contact);stroke:#fff;stroke-width:1.1;vector-effect:non-scaling-stroke}
    .an-place-ring{fill:none;stroke:var(--an-contact);stroke-width:1;opacity:.4;vector-effect:non-scaling-stroke}
    @keyframes anFlow{to{stroke-dashoffset:-6}}
    @keyframes anBuzz{from{transform:scaleX(.72)}to{transform:scaleX(1.18)}}
    @keyframes anPulse{0%,100%{opacity:.35;transform:scale(.8)}50%{opacity:1;transform:scale(1.35)}}
    @keyframes anBurst{0%,38%{opacity:.25;transform:scale(.75)}48%{opacity:1;transform:scale(1.5)}70%,100%{opacity:.3;transform:scale(.9)}}
    @keyframes anNasalFlow{to{stroke-dashoffset:-7}}
    .anatomy-mini{
      width:100%;max-width:360px;display:block;margin:10px auto 4px;
      border:1px solid var(--line);border-radius:12px;background:linear-gradient(180deg,var(--card),var(--bg));padding:8px
    }
    .anatomy-mini .an-labels text{font-size:3.25px}
    .anatomy-mini[data-active="1"] .an-place-ring{transform-origin:center;transform-box:fill-box;animation:anPulse 1.25s ease-in-out infinite}
    .anatomy-mini[data-manner="plosive"][data-active="1"] .an-place-ring,
    .anatomy-mini[data-manner="affricate"][data-active="1"] .an-place-ring{animation:anBurst 1.65s ease-in-out infinite}
    .anatomy-mini[data-active="1"] .an-oral-air{opacity:.75;animation:anFlow .55s linear infinite}
    .anatomy-mini[data-manner="nasal"] .an-oral-air{opacity:.12}
    .anatomy-mini[data-manner="nasal"][data-active="1"] .an-nasal-air{opacity:.9;animation:anNasalFlow .65s linear infinite}
